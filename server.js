/**
 * Server Entry Point
 * 
 * This file starts the Express server for the application.
 */

// Import the Express app from the API routes
const app = require('./src/api');

// The server is already configured and started in the imported file
console.log('Server started. See details above.');
